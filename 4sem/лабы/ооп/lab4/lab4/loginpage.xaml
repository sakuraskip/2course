<Window x:Class="lab4.loginpage"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:local="clr-namespace:lab4"
        xmlns:vm="clr-namespace:lab4.ViewModels"
        xmlns:md="http://materialdesigninxaml.net/winfx/xaml/themes"
        mc:Ignorable="d"
        Title="Вход" Height="700" Width="900"
        WindowStartupLocation="CenterScreen">

    <Window.DataContext>
        <vm:LoginViewModel/>
    </Window.DataContext>

    <Window.Resources>
        <SolidColorBrush x:Key="OceanBlue" Color="#1E88E5"/>
        <SolidColorBrush x:Key="LightWave" Color="#E3F2FD"/>

        <Style TargetType="TextBox" BasedOn="{StaticResource MaterialDesignFloatingHintTextBox}">
            <Setter Property="Margin" Value="0,10,0,20"/>
            <Setter Property="FontSize" Value="18"/>
            <Setter Property="md:HintAssist.FloatingScale" Value="0.8"/>
            <Setter Property="Background" Value="White"/>
            <Setter Property="md:HintAssist.Foreground" Value="{StaticResource OceanBlue}"/>
        </Style>

        <Style TargetType="Button" BasedOn="{StaticResource MaterialDesignRaisedButton}">
            <Setter Property="Background" Value="{StaticResource OceanBlue}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Height" Value="50"/>
            <Setter Property="FontSize" Value="18"/>
            <Setter Property="Margin" Value="0,0,0,20"/>
        </Style>

        <Style x:Key="WaveStyle" TargetType="Path">
            <Setter Property="Stretch" Value="Fill"/>
            <Setter Property="Height" Value="30"/>
            <Setter Property="Fill" Value="{StaticResource OceanBlue}"/>
            <Setter Property="Opacity" Value="0.1"/>
            <Setter Property="HorizontalAlignment" Value="Stretch"/>
        </Style>
    </Window.Resources>

    <Grid Background="{StaticResource LightWave}">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!--<Path Grid.Row="0" Style="{StaticResource WaveStyle}"
              Data="M0,20 Q100,0 200,20 T400,20 T600,20 T800,20 T900,20 L900,0 L0,0 Z"/>-->

        <Grid Grid.Row="1" Grid.RowSpan="3">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="400"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Viewbox Grid.Column="1" VerticalAlignment="Top" Margin="0,100,0,30">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="{DynamicResource LoginTitle}" 
                               Style="{StaticResource MaterialDesignHeadline4TextBlock}"
                               Foreground="{StaticResource OceanBlue}"/>
                </StackPanel>
            </Viewbox>

            <md:Card Grid.Column="1" Grid.Row="1" UniformCornerRadius="6" Padding="30"
                     Background="White"
                     BorderBrush="{StaticResource OceanBlue}" BorderThickness="0 0 0 3"
                     VerticalAlignment="Center">
                <StackPanel>
                    <TextBox Text="{Binding Login, UpdateSourceTrigger=PropertyChanged}"
                             md:HintAssist.Hint="Логин"/>

                    <PasswordBox x:Name="PasswordBox"
                                 md:HintAssist.Hint="Пароль"
                                 Margin="0,10,0,30"
                                 PasswordChanged="PasswordBox_PasswordChanged"/>

                    <Button Command="{Binding LoginCommand}"
                            Content="Войти"/>

                    
                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                        <TextBlock Text="Нет аккаунта?" 
                                   VerticalAlignment="Center"
                                   Margin="0,0,5,0"/>
                        <TextBlock>
                            <Hyperlink Command="{Binding RegisterCommand}"
                                       Foreground="{StaticResource OceanBlue}"
                                       TextDecorations="None">
                                Зарегистрироваться
                            </Hyperlink>
                        </TextBlock>
                    </StackPanel>
                </StackPanel>
            </md:Card>
        </Grid>

        <!--<Path Grid.Row="4" Style="{StaticResource WaveStyle}"
              Data="M0,0 Q100,20 200,0 T400,0 T600,0 T800,0 T900,0 L900,20 L0,20 Z"/>-->
    </Grid>
</Window>